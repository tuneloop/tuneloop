---
name: cut-release
description: Cut a new tuneloop release. Use when the user wants to "cut a release", "create a release", "publish a new version", or bump the version.
---

# Cut a release

Create a new release with the changes:

- make sure you are on latest main
- see changes since last release
- draft changelog
- update package.json and lock files with the new appropriate version
- open a PR
- once the PR is merged, create a github release tag.
