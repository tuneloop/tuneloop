node_modules/
dist/
*.tsbuildinfo
coverage/

# tuneloop's local data store (default location when run from a repo)
.tuneloop/
# Local SQLite stores dropped in the working tree (contain private session data)
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal

# skill-creator eval workspaces (transient benchmark artifacts)
skills/*-workspace/

# Claude Code local (machine-specific) settings; shared .claude/settings.json is fine to commit
.claude/settings.local.json

# Local secrets / env
.env
.env.*
.tuneloop.local.env

# OS / editor
.DS_Store
*.swp
*.log
.cursorindexingignore
.specstory/

# Local prototype/experiment scratch
experiments/
.gstack/
